import {names} from "./modules/index.js";   // list of names array (forEach)
console.log (names);

import person from "./modules/app.js"      // person detail (Objects person.name)
console.log (person);

import {alian} from "./foldertwo/index.js"   // if and else if statements (print true)
console.log (alian);

import func from "./foldertwo/app.js"      // function parameters & arguments (Greeting)
console.log (func)
